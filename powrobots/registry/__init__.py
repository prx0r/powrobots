# STALE — This package was intended to hold source registry logic.
# It was never populated. The registry logic lives in shared/db.py
# (seed_registry(), SOURCES_SEED).
# Either remove this package or migrate the registry logic here.
